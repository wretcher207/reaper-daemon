Netlify publishes this dir, but netlify.toml 301s /* to deadpixeldesign.com, so nothing here is served.
The old product page lives in git history (last touched in the "site: full product page rebuild" commit).
